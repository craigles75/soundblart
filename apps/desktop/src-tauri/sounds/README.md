# sounds/

Bundled preset sound files (.wav) that are copied to the platform app-support
directory on first launch via `presets::installer`.

Subdirectory names become panel names in the UI. Only `.wav` files are loaded.

Example layout:

```
sounds/
  Ambient/
    rain.wav
    thunder.wav
  Bells/
    ding.wav
```
